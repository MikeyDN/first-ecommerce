import promoted from './promoted'
import product from './product'
import category from './category'
import purchases from './purchases'

export const schemaTypes = [promoted, product, category, purchases]